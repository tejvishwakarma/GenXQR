# Root .env — credentials for the local dev docker-compose infra (Postgres + Redis).
# Copy to .env and adjust as needed. These are LOCAL DEV values only.
POSTGRES_USER=genxqr
POSTGRES_PASSWORD=change_me_dev
POSTGRES_DB=genxqr
REDIS_PASSWORD=change_me_dev
POSTGRES_PORT=5433
REDIS_PORT=6380
